module.exports = function(config){
	return(function(module, callback){
		// watch all templates/*.ejs.html files. 
		// if file changes then do off all templates

		// watch all js/*.js
		// if file change, then do full build

		callback && callback();

	});
};